# GoExample 待优化清单（V19）

评估日期：2026-09-03  
状态：**仓库内可执行项已完成；目标环境证据待获取**  
范围：根工作区、`Framework`、`Solutions/Example`、`Services/Billing`、协议、测试、CI、供应链、观测与部署；明确排除 `MSFront`。  
基线综合评分：**9.846/10**（未舍入加权值 **9.8456/10**）。当前综合评分：**9.846/10**（未舍入加权值 **9.8456/10**，A−）。V19-01 已完成并通过验证；由于创建本清单时未因该风险扣分，且证据完整性维度已经为 10.00，本次不重复加分。

## 1. 汇总结论

V14 至 V18 的仓库内实现项已经完成：标准 HTTP/Fiber 适配与热路径、队列 header 路径、HTTP 出站复制/响应体限制/retry/circuit breaker、标准应用入口、路由与 SSE 描述符、SQL 事务在 `BeginTx` 后和 `Commit` 前的取消保护均已有实现与相应自动测试。它们不再次列为待开发项目。

历史未完成项均为目标环境、远端制品、长稳运行或正式发布证据，过去在 V14、V15、V16、V17、V18 中多次迁移。本清单按能力域去重后完整承接，保留原始编号映射，避免将同一项误算为多个未完成项。

重新评估识别出的仓库内缺口已经关闭：`scripts/evidence-manifest.mjs` 与 `scripts/evidence-verify.mjs` 现在共用 `scripts/lib/bounded-command.mjs`，同步采用 64 MiB 有界输出容量，并共用 Git 状态 dirty/count/SHA-256 摘要逻辑。真实子进程的 `1 MiB + 8 KiB` 输出、非零退出和空/非空状态哈希一致性均有行为测试；当前工作树的 manifest/verify 端到端命令已通过，不再复现约 1 MiB 处的 `ENOBUFS`。

## 2. 待优化项目

| 编号 | 优先级 | 状态 | 历史来源 | 待完成内容 | 完成与计分边界 |
| --- | --- | --- | --- | --- | --- |
| V19-01 | P0 | 仓库内完成 | 本次独立评估 | Evidence manifest 大工作树鲁棒性 | 生成器与验证器共用 64 MiB 有界命令执行和 Git 状态摘要；已覆盖超 1 MiB 输出、命令失败、dirty/count/SHA-256 一致性，并通过完整 manifest/verify。该修复关闭风险，但不重复增加创建 V19 时已保持 10.00 的证据完整性评分。 |
| V19-02 | P0 | `not_recorded` | V14-03、V14-06、V14-12；V15-06；V16-06；V17-02；V18-02 | 固定 Linux transport 与跨框架可比基准 | 在同一不可变 Linux runner、CPU、Go、`GOMAXPROCS`、连接模型和 payload 下，比较 Fiber、`net/http`、Framework 标准入口及选定外部框架；保存原始样本、环境指纹、p50/p95/p99、吞吐、错误率、CPU、GC、RSS、FD 和容量拐点。超过预设 5% 回退阈值必须失败，并形成 transport 决策 ADR。 |
| V19-03 | P0 | `not_recorded` | V14-04 目标环境部分、V14-05、V14-12；V15-07；V16-07；V17-02；V18-02 | 真实业务负载与长稳容量 | 覆盖真实 JSON、上传下载、认证、PostgreSQL、Redis、RPC/queue、TLS edge、SSE 与慢客户端；记录稳态和饱和阶段 p95/p99、错误率、CPU、GC pause/alloc rate、RSS、goroutine、连接池、FD、队列积压及至少 30 分钟趋势，得出有余量的安全并发/RPS 和错误预算。 |
| V19-04 | P0 | `not_recorded` | V14-07；V15-08；V16-08；V17-02；V18-02 | 目标 edge 与网络协议实证 | 在目标 DNS、真实证书和真实 edge/ingress 上验证 TLS 1.2/1.3、HTTP/2/3、header/body/buffer 限制、超时、客户端断连、streaming、drain、发布 rollback；归档配置版本、原始探测输出、run URL、哈希和独立 verifier 结果。 |
| V19-05 | P0 | `not_recorded` | V14-08；V15-09；V16-09；V17-02；V18-02 | Redis 高可用与恢复 | 验证真实 Redis TLS、独立 ACL、Sentinel 或 Cluster 的 failover、重连、网络分区、滚动升级、容量、告警、备份恢复，并取得已批准的 RPO/RTO。仓库内 test double 和本地非 TLS 合约不能替代该证据。 |
| V19-06 | P0 | `not_recorded` | V14-09、V14-10；V15-10；V16-10；V17-02；V18-02 | 正式身份、密钥与审计运营 | 验证正式 IdP、MFA、claim mapping、key rotation、集中 revoke、设备治理、KMS/Vault、生产 policy engine；同时验证审计 sink 的加密、访问控制、持久化、留存/删除、SIEM ingestion、paging、具名责任人与处置演练。 |
| V19-07 | P1 | `not_recorded` | V14-11；V15-11；V16-11；V17-02；V18-02 | 真实 PostgreSQL/queue 故障与数据恢复 | 对真实业务依赖验证 deadlock、连接耗尽、lease、资源耗尽、跨主机/跨区分区、磁盘损坏、WAL/PITR、broker 恢复及一致性边界。需要独立区分至少一次、幂等和 exactly-once，不能以仓库内 SQL/NATS 合约替代。 |
| V19-08 | P1 | `not_recorded` | V14-13；V15-12；V16-12；V17-02；V18-02 | 远端制品、供应链与 Kubernetes 发布 | 取得远端 attestation、镜像签名、registry provenance、部署验签，以及目标集群 dry-run、rollout、HPA、驱逐、PDB、NetworkPolicy、rollback 的不可变输出和 verifier 结果。 |
| V19-09 | P2 | `not_recorded` | V14-14；V15-13；V16-13；V17-02；V18-02 | 正式 SDK 与外部消费验证 | 发布正式 SDK tag，执行外部消费者跨版本矩阵与弃用窗口验证，完成目标部署迁移，并以第二个真实 Framework 服务验证可复用边界。 |
| V19-10 | P1 | `not_recorded` | V14-15；V15-14；V16-14；V17-02；V18-02 | 跨依赖长稳与事故响应 | 在目标 Redis/PostgreSQL/broker/collector/edge 上执行长稳、磁盘满、证书轮换、跨主机/跨区域分区、资源耗尽、告警响应和恢复演练；输出趋势、告警时效、责任链与已批准 RPO/RTO。 |

## 3. 历史迁移完整性

| 历史未完成能力 | V19 承接项 | 处理结论 |
| --- | --- | --- |
| V14-03、V14-06、V14-12，及其 V15/V16/V17/V18 迁移项 | V19-02 | 固定 Linux、三路 transport、跨框架对照和选型证据统一验收。 |
| V14-04 的目标环境测量部分、V14-05，及其迁移项 | V19-03 | 仓库内中间件优化已完成；真实依赖、容量和 soak 仍未记录。 |
| V14-07 至 V14-15，及其 V15-08 至 V15-14、V16-08 至 V16-14、V17-02、V18-02 迁移项 | V19-04 至 V19-10 | 按 edge、Redis、身份审计、数据恢复、发布供应链、SDK、长稳事故域去重承接。 |
| V17-02、V18-02 | V19-02 至 V19-10 | 两项均是上述目标环境工作包的聚合描述，不另行重复计数。 |

## 4. 执行顺序

1. V19-01 已完成：本地 evidence manifest/verify 对大工作树恢复可用，并由行为测试锁定输出上限、失败和哈希边界。
2. 并行准备 V19-02 至 V19-06 所需的固定 runner、真实依赖、目标 edge、身份平台与审计权限。
3. 在真实负载基线形成后执行 V19-03、V19-04、V19-05、V19-06；不能用本机微基准或 mock 替代。
4. 再执行 V19-07、V19-08、V19-09、V19-10 的恢复、发布、消费者和长稳工作包；每项独立归档原始输出、环境指纹、不可变 run URL、制品 SHA-256、审批和独立 verifier。

## 5. 验收与评分规则

- 只有已实现能力及其自动化或目标环境证据通过，才调整分项和综合评分；文档、任务迁移、API 数量与证据输入数量均不加分。
- `not_recorded` 表示缺少目标环境不可变证据，不表示已完成，也不以模拟输出、手工描述或本地 test double 代替。
- V19-01 是仓库内工作且已经完成；其余项目需要真实基础设施、权限或外部组织协作，在未取得条件前保持 `not_recorded`。
- `MSFront` 不修改、不重评、不计入本版本任何评分变化。

## 6. 本轮记录

V19-01 将 manifest 与 verifier 原本各自复制的同步命令策略收敛为 `scripts/lib/bounded-command.mjs`。共享执行器保留 `shell: false`、Windows 隐藏窗口和既有命令候选/回退语义，把 Node 默认约 1 MiB 缓冲提高为经过显式约束的 64 MiB；该上限可以容纳数十万条常见 porcelain 状态记录，同时避免无界内存。共享 `summarizeGitStatus` 统一计算 dirty、变更文件数和原始 UTF-8 状态的 SHA-256，防止生成与验证逻辑漂移。

`__test__/node/script-guards.test.mjs` 新增真实子进程超 1 MiB 输出、非零退出、空/非空状态计数和哈希一致性测试，并让 evidence 测试自行创建/清理库存制品，不再依赖本机残留状态。`scripts/lib/bounded-command.mjs`、V17、V18 和 V19 文档已加入 evidence 输入合约。

验证结果：新增聚焦测试通过；脚本守卫 37/37 通过；`yarn test:node` 主阶段 156/156、独立 release 阶段 1/1，总计 157 个测试通过；`yarn evidence:manifest` 与 `yarn evidence:verify` 通过。首次 release 验证发现系统环境把 Go 1.27.1 的 `GOROOT` 强制注入仓库固定的 Go 1.25.13 二进制，清除该无效外部覆盖后固定工具链使用自身 GOROOT 并通过；这属于本机环境污染，不改写为项目缺陷或目标环境成功。V19-02 至 V19-10 未获得真实环境、设备、权限或外部组织证据，继续保持 `not_recorded`，未用 mock 或本机输出替代。

评分已按本次优化更新：未舍入加权值 **9.8456 → 9.8456（+0.0000）**，三位综合评分保持 **9.846/10**，等级保持 **A−**。原因是 V19 基线明确未对已发现的缓冲风险扣分，且证据完整性与生产就绪维度已经是 10.00；再次加分会形成双重计分。

## 7. 验证命令

```powershell
node --test --test-concurrency=1 __test__/node/script-guards.test.mjs
Remove-Item Env:GOROOT -ErrorAction SilentlyContinue
yarn test:node
yarn evidence:manifest
yarn evidence:verify
```
