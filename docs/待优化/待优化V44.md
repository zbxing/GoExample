# GoExample 待优化清单（V44）

生成日期：2026-09-05
范围：非前端目录；`MSFront` 完全排除，不评估、不修改、不计分。
状态：V43 仓库内项目已关闭；V44-01 仓库内完成；V44-02/V44-03 继续 `not_recorded`。

## 1. 互斥流程判断

V43-01（server recovery evidence 外层 300 秒 timeout、`SIGTERM`、非 shell 和结果归一化）已经完成并通过测试、静态守卫、证据 verify 与 manifest 验收。V43-02/V43-03 需要真实目标环境、生产依赖、外部制品或组织批准，属于不可抗因素，不能由本地模拟替代。

因此本轮先完成 V44 评估和清单，再实施 V44-01。清单生成前不修改代码。

## 2. 复评发现

七个非 MSFront 同步 evidence 入口分别直接调用 `spawnSync`：

- `scripts/authorization-evidence.mjs`
- `scripts/audit-chain-evidence.mjs`
- `scripts/oidc-browser-evidence.mjs`
- `scripts/sdk-consumer-evidence.mjs`
- `scripts/sdk-release-evidence.mjs`
- `scripts/sdk-consumer-matrix-evidence.mjs`
- `scripts/server-recovery-drill.mjs`

这些入口已有 timeout、`shell: false`、`windowsHide: true` 和 `maxBuffer`，但没有显式 `killSignal: 'SIGTERM'`，依赖 Node 默认值。默认值虽然当前也是 SIGTERM，却不是入口层可审计合同，未来修改或跨平台差异可能造成终止语义漂移。该问题属于仓库内证据生命周期，可以在不改变 report/schema/artifact/cleanup 的前提下修复。

已排除的候选项：Go/Node metadata probe 和 Kubernetes/Prometheus/workflow lint 已使用共享命令边界；运行时热路径、生产依赖、目标部署、真实硬件、正式身份/密钥、远端 provenance、PITR/RPO/RTO 不能本地替代。

## 3. 待优化项目

| 编号 | 优先级 | 状态 | 实施内容 | 完成定义 |
| --- | --- | --- | --- | --- |
| V44-01 | P1 | 仓库内完成 | 在上述七个同步 evidence 入口显式加入 `killSignal: 'SIGTERM'`，并扩展静态守卫检查每个入口的 timeout、非 shell、隐藏窗口、输出上限和终止信号。 | 七个入口均已具备完整进程边界；成功、timeout、signal、spawn failure、缺失 status、非零退出仍不能声明 `passed`；现有 command、timeout、report schema、artifact 集合和 cleanup 不变。 |
| V44-02 | P0 | `not_recorded` | 真实 Linux/硬件、目标 edge/Kubernetes、生产 Redis/PostgreSQL/NATS、正式 IdP/KMS/SIEM、远端签名制品、正式 SDK tag 和外部 consumer。 | 获得不可变目标环境 run URL、原始输出、摘要和独立 verifier 前保持 `not_recorded`。 |
| V44-03 | P1 | `not_recorded` | 生产 PITR、跨主机恢复、批准的 RPO/RTO、跨依赖故障响应与组织演练。 | 获得生产数据路径、批准目标值、授权窗口和独立见证前保持 `not_recorded`。 |

## 4. 执行顺序与验收

1. 仅修改七个非 MSFront evidence 入口和现有静态守卫测试，保持命令参数与报告合同不变。
2. 运行 Node 门禁，确认静态合同覆盖七个入口且所有既有行为测试通过。
3. 重新生成 server recovery evidence，执行独立 verify；重新生成 evidence manifest 并执行独立 manifest verify。
4. 运行 Go test/race/vet/build、API compatibility、SDK check、govulncheck、`git diff --check` 和 `graphify update .`。
5. 依据实际结果回写本清单与评估文档：V44-01 已标记完成，不把 V44-02/V44-03 的外部证据缺失计为完成。

## 5. V44 实施与验收记录

1. 七个入口均已显式设置 `killSignal: 'SIGTERM'`，静态守卫覆盖 timeout、`shell:false`、`windowsHide:true`、`maxBuffer` 和终止信号。
2. Node 门禁 **209/209**、server-release **5/5**；Go test/race/vet/build、API compatibility **356 symbols**、SDK check **26 operations**、govulncheck 和 `git diff --check` 均通过。
3. 七类 evidence 重新生成并独立 verify；server recovery 四场景通过；manifest 为 **308 inputs / 62 artifacts**。
4. `graphify update .` 已在最终文档和代码稳定后执行，图谱已更新。

## 6. 评分基线与目标

V43 完成评分为 **9.8666/10（A-）**。V44-01 只改善“韧性、故障与恢复”维度，复评基线为 9.92，目标为 9.95；按 94% 纳入维度归一化后，目标综合评分约为 **9.8676/10（A-）**。不因 fixture、文档、manifest 数量或等待时长增加而加分，也不把 V44-02/V44-03 的外部证据缺失计为完成。

## 7. 关闭条件

- 七个入口均显式设置 `killSignal: 'SIGTERM'`，静态守卫覆盖完整选项集合，已完成。
- timeout、signal、spawn error 或非零退出不得生成 passed 结论。
- server recovery 的场景预算、外层预算、report/schema、artifact 顺序和 cleanup 语义不变。
- Node/Go/evidence/manifest/图谱门禁均已有可复核输出，V44-01 已标记“仓库内完成”并更新评分。
- V44-02/V44-03 在取得真实目标环境证据前保持 `not_recorded`。
- `MSFront` 不得出现在本轮代码修改、评估或计分中。
