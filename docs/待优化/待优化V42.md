# GoExample 待优化清单（V42）

生成日期：2026-09-05
范围：非前端目录；`MSFront` 完全排除，不评估、不修改、不计分。
状态：V42-01 仓库内优化已完成；V42-02/V42-03 继续保持 `not_recorded`。
前置结论：V41-01 已完成，本轮先完成 V42 评估与清单，再实施并验收 V42-01。

## 1. 互斥流程判断

V41 中唯一的仓库内项是 PostgreSQL recovery 合同共享 runner 迁移，已完成并通过静态守卫、Node 门禁和 evidence manifest 验收。V41-02 至 V41-10 需要真实 Linux/硬件、生产依赖、目标 edge/Kubernetes、正式身份/密钥、远端制品、外部 SDK 消费者或组织审批，继续保持 `not_recorded`。

因此本轮采用“先重评估、生成新清单、再实施”的互斥分支。重新扫描非 MSFront 脚本后，发现三个同步证据入口仍直接调用 `spawnSync()`，虽有 `shell:false`、`windowsHide:true` 和输出上限，却没有 `timeout` 与 `killSignal`。该缺口可以在仓库内修复，登记为 V42-01。

## 2. 待优化项目

| 编号 | 优先级 | 状态 | 具体工作 | 完成定义与评分边界 |
| --- | --- | --- | --- | --- |
| V42-01 | P1 | 仓库内完成 | 新增共享 `scripts/lib/evidence-command.mjs`；迁移 `kubernetes-evidence.mjs`、`prometheus-rules.mjs`、`workflow-lint.mjs`；统一每个 stdout/stderr 流 2 MiB 输出上限、显式 timeout、`SIGTERM`、非 shell、Windows 隐藏窗口和状态归一化；增加行为测试与静态守卫。 | 三个入口不再直接导入/调用 `spawnSync()`；Kubernetes 使用 30 秒预算，Prometheus/workflow lint 使用 180 秒预算；timeout、signal、spawn/missing-status/nonzero 均形成确定失败结果；现有报告 schema、命令顺序、fixture、cleanup 不变。仅改善证据控制面的韧性与可审计性。 |
| V42-02 | P0 | `not_recorded` | 固定 Linux transport 与跨框架基准、真实硬件、真实业务长稳/容量、目标 edge/HTTP3、生产 Redis/PostgreSQL/NATS、正式 IdP/KMS/SIEM、目标 Kubernetes rollout/HPA/PDB/NetworkPolicy/rollback、远端签名/provenance、正式 SDK tag 与外部消费者。 | 需要不可替代的真实环境、凭据、原始输出、环境指纹、不可变 URL 和独立见证；本地模拟不计分。 |
| V42-03 | P1 | `not_recorded` | 生产 PITR、跨主机恢复、批准的 RPO/RTO、跨依赖事故响应与组织演练。 | 需要生产数据路径、批准目标值和组织授权；保持 `not_recorded`。 |

## 3. V42-01 实施顺序

1. 编写共享同步 runner，限制命令超时、每流输出、shell 与终止信号，并保留 `status/stdout/stderr` 兼容形状。
2. 迁移三个脚本：Kubernetes renderer 为 30 秒，promtool/actionlint/Go build 与 lint 为 180 秒；不改变现有证据报告字段。
3. 增加 `evidence-command.test.mjs`，覆盖安全选项、timeout、signal、spawn/missing-status/nonzero 和输出归一化；在 `script-guards.test.mjs` 锁定三个入口的导入与调用边界。
4. 将 helper、测试、V42 评估和清单加入 `evidenceInputPaths`，重建 manifest 与本地 evidence artifacts。
5. 运行 Node 非 server-release 门禁、server-release 静态/可运行测试、Go/API/SDK 门禁、evidence verify、`git diff --check` 和 `graphify update .`。

## 4. 验收记录

V42-01 已完成并记录如下：

- `scripts/lib/evidence-command.mjs` 已集中实现 180 秒最大预算、每流 2 MiB `maxBuffer`、`shell:false`、`windowsHide:true` 和 `SIGTERM`；三个入口不再直接导入/调用 `spawnSync()`，Kubernetes renderer 使用 30 秒预算。
- `evidence-command.test.mjs`、`script-guards.test.mjs` 行为与静态守卫通过；非 server-release Node 门禁 **205/205**。
- Kubernetes、Prometheus、workflow lint evidence 均生成并独立 verify；总 manifest **304 inputs / 62 artifacts**，`evidence:verify` 通过。
- Go test/race/vet/build、govulncheck、API compatibility **356 symbols**、SDK check **26 operations** 和 `git diff --check` 通过；`graphify update .` 已执行。
- server-release 真实构建/可复现子测试在当前 Windows Go 工具链外层 184 秒预算内超时，已终止本次测试子进程并保持环境阻塞记录；不宣称通过，也不影响 V42-01 的仓库内证据 runner 验收。

## 5. 评分基线与目标

V42 复评基线沿用 V41-01 完成值 **9.8647/10（A-）**。V42-01 已完成，只调整“韧性、故障与恢复”维度（9.86 → 9.90），94% 归一化综合评分更新为 **9.8660/10（A-）**，相对基线 **+0.0013**。不因文档、测试数量、manifest 数量、本地 fixture 或命令等待时间把运行时性能、生产就绪、身份治理或目标部署记为完成。

## 6. 关闭条件

V42-01 的代码、测试、manifest、evidence verify 与图谱更新均已完成；当前没有发现新的可在仓库内诚实推进且不改变外部边界的缺口。V42-02/V42-03 在获得真实证据前不得标记完成；`MSFront` 不得出现在本轮修改、评估或评分中。
