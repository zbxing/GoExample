# GoExample 待优化清单（V43）

生成日期：2026-09-05
范围：非前端目录；`MSFront` 完全排除，不评估、不修改、不计分。
状态：V42 仓库内项目已关闭；V43-01 仓库内完成；V43-02/V43-03 继续 `not_recorded`。

## 1. 互斥流程判断

V42-01（三个同步证据入口共享命令边界）已经完成并通过测试、静态守卫、证据 verify 与 manifest 验收。V42-02/V42-03 需要真实目标环境、生产依赖、外部制品或组织批准，属于不可抗因素，不能由本地模拟替代。因此先完成 V43 评估和清单，再实施 V43-01。

## 2. 复评发现

`server-recovery-drill.mjs` 对四个恢复场景分别设置 60 秒 Node 上限，Go 测试使用 45 秒预算；但 `server-recovery-evidence.mjs` 外层启动 drill 时没有总 timeout 或 `SIGTERM`。该问题属于仓库内证据编排生命周期，可通过增加有限的 300 秒外层预算修复。

## 3. 待优化项目

| 编号 | 优先级 | 状态 | 实施内容 | 完成定义 |
| --- | --- | --- | --- | --- |
| V43-01 | P1 | 仓库内完成 | 给外层 drill 增加 300 秒总 timeout、`SIGTERM`、非 shell、隐藏窗口和明确结果归一化。 | timeout、signal、缺失 status、非零退出均明确分类，不能宣称 passed；report/schema/artifact/cleanup 不变。 |
| V43-02 | P0 | `not_recorded` | 真实 Linux/硬件、目标 edge/Kubernetes、生产依赖、正式 IdP/KMS/SIEM、远端制品和外部 consumer。 | 获得不可替代目标环境证据前保持 `not_recorded`。 |
| V43-03 | P1 | `not_recorded` | 生产 PITR、跨主机恢复、批准 RPO/RTO、跨依赖故障响应和组织演练。 | 获得生产路径和独立见证前保持 `not_recorded`。 |

## 4. 实施与评分

V43-01 已增加 300 秒总 timeout、`SIGTERM`、`shell:false`、`windowsHide:true` 和 execution 结果归一化，并通过行为测试、静态守卫、server recovery evidence verify、manifest 验收及全量门禁。V43-01 只提升“韧性、故障与恢复”维度，综合评分由 V42 的 **9.8660/10（A-）** 提升为 V43 的 **9.8666/10（A-）**。

## 5. 关闭条件

- V43-01 的代码、测试、证据和图谱更新均已完成。
- V43-02/V43-03 在取得真实目标环境证据前保持 `not_recorded`。
- `MSFront` 不得出现在本轮代码修改、评估或计分中。
