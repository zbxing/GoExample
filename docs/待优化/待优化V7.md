# 待优化 V7

评估日期：2026-08-17  
来源：V6 后全仓复扫、PostgreSQL migration 验证边界、OpenAPI 引用完整性和供应链安全门禁复核  
目标：把数据库与供应链能力推进到可在远端 CI 重复执行的验证闭环

## 1. 本轮已完成

### V7-01 PostgreSQL CI service integration

- 优先级：P0
- 状态：`[x]`（实现完成，远端记录待补）
- 修改：
  - MSFront quality job 新增固定 OCI digest 的 PostgreSQL 16 service。
  - service 使用独立 CI 数据库、健康检查和最小测试凭据。
  - 新增 `yarn test:database` 根命令和 MSFront 子命令。
- 验收：workflow 通过 actionlint；本机无 PostgreSQL/Docker，因此没有本地 DDL 运行记录。

### V7-02 migration 并发、幂等与约束集成测试

- 优先级：P0
- 状态：`[x]`（测试实现完成，目标数据库执行待补）
- 修改：
  - 新增 `__test__/integration/msfront/database-migration.mjs`。
  - 两个独立进程同时执行 migration，验证 advisory lock 串行化。
  - 再次执行验证 `already applied`，`--status` 验证 applied 状态。
  - 查询 migration history、7 张基础设施/业务表和 project code 唯一索引。
  - 在事务中插入大小写不同的重复 code，要求 PostgreSQL 返回 `23505`，随后回滚。
- 验收：无连接时明确输出 skipped；CI 环境变量存在时任何连接或断言失败都会使 job 失败。

### V7-03 OpenAPI 引用与 security 完整性

- 优先级：P1
- 状态：`[x]`
- 修改：
  - 递归收集 OpenAPI 所有本地 `$ref`，逐个按 JSON Pointer 解析。
  - 每个 operation security requirement 必须引用已声明 security scheme。
  - 保留 Fiber runtime method/path、operationId 和 responses 契约。
- 验收：OpenAPI parser 套件增至 4 个用例，MSFront 总计 42 个单测通过。

### V7-04 CodeQL 双语言分析

- 优先级：P1
- 状态：`[x]`（实现完成，远端记录待补）
- 修改：
  - 新增 `security-analysis.yml`。
  - Go 使用 autobuild，JavaScript/TypeScript 使用 no-build analysis。
  - CodeQL Action v4.37.7 固定完整 commit SHA。
  - 权限限定为 contents/packages read 与 security-events write。
- 验收：actionlint 通过；只有 GitHub Code Scanning 记录可证明实际分析完成。

### V7-05 Dependency Review 与 Dependabot

- 优先级：P1
- 状态：`[x]`（实现完成，远端记录待补）
- 修改：
  - PR 中依赖/lock/Docker/workflow 变化触发 dependency review，High 及以上失败。
  - Dependabot 每周检查根/MSFront npm、两个 Go module、GitHub Actions 和 Example Docker。
  - 限制各生态同时打开的 PR 数，避免更新队列失控。
- 验收：配置与固定 action SHA 通过静态验证。

### V7-06 不可变 workflow 引用守卫

- 优先级：P1
- 状态：`[x]`
- 修改：
  - 根 Node 测试扫描全部 workflow。
  - 所有 `uses:` 必须使用 40 位 commit SHA。
  - 所有 service `image:` 必须带 64 位 `sha256` digest。
- 验收：根 Node 测试增至 5 个用例并通过；actionlint 验证 6 个 workflow。

## 2. 本轮验证结果

| 检查 | 结果 |
| --- | --- |
| `yarn test` | 根 Node 5 用例、Go 全包、MSFront 11 文件/42 用例全部通过 |
| Route Handler contract | 37 个受保护方法通过 |
| OpenAPI contract | 17 个 Fiber 路由、17 个 operationId、所有本地引用和 security scheme 可解析 |
| `yarn test:database` | 本机无数据库，明确 skipped；CI PostgreSQL service 已配置 |
| `yarn race:server` | Windows CGO + GCC 通过 |
| `yarn vet:server` | 通过 |
| `yarn vuln:server` | 两个 module 均为 0 个可达漏洞 |
| lint/typecheck/build | MSFront 全部通过 |
| production audit | 73 个生产依赖，0 漏洞 |
| actionlint | 6 个 workflow 通过 |

## 3. 仍需完成

### V7-07 获取远端 PostgreSQL 与安全扫描记录

- 优先级：P0
- 状态：`[-]`
- 要求：运行并保存 MSFront PostgreSQL integration、CodeQL、dependency review、SBOM/Grype 的 GitHub Actions 记录和 artifact；失败时修正真实兼容问题。

### V7-08 PostgreSQL repository 与恢复闭环

- 优先级：P0
- 状态：`[-]`
- 要求：Route Handler + repository CRUD/事务故障注入、连接耗尽、statement timeout、死锁/serialization retry、备份、PITR、RPO/RTO 和恢复演练。

### V7-09 正式身份与共享状态

- 优先级：P0
- 状态：`[-]`
- 要求：OIDC/OAuth2、MFA、集中撤销、密钥轮换和 limiter/idempotency/lock 共享 store。

### V7-10 可观测性、浏览器与容量

- 优先级：P1
- 状态：`[-]`
- 要求：OpenTelemetry/SLO、Playwright/axe/Web Vitals/bundle budget、Linux/TLS/数据库压测、长稳和故障注入。

### V7-11 发布证明

- 优先级：P1
- 状态：`[-]`
- 要求：镜像 SBOM/扫描、cosign 签名、SLSA provenance、部署、回滚和恢复证明。

## 4. 评分影响

- HTTP 与 API 契约：9.8 -> 9.9。
- 安全与身份：8.5 -> 8.6。
- 数据一致性与持久化：8.5 -> 8.8。
- 测试与静态质量：9.4 -> 9.5。
- CI 与供应链：9.2 -> 9.5。
- 扩展与部署能力：7.4 -> 7.5。
- 文档与可维护性：9.6 -> 9.7。
- 综合加权分：8.993 -> 9.071，按一位小数从 **9.0/10** 提升到 **9.1/10**。

## 5. 进入 V8 的准入条件

优先执行 V7-07，获取远端 workflow 真实结果。V8 应至少完成 PostgreSQL repository integration、浏览器 E2E、OpenTelemetry 或目标环境容量中的一项，不再仅通过增加静态 workflow 提分。
