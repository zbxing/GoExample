# 项目待优化 V3

> 形成日期：2026-08-17  
> 来源：全仓库代码、配置、依赖、测试、基准与网络资料复核  
> 基线：V2 完成后的单 module 服务端、根 Node 编排项目和 `MSFront`  
> 目标：每个分项和综合评分最高 10 分；不以未验证能力换取分数  
> 状态：`[ ]` 未开始，`[~]` 进行中，`[x]` 已完成，`[-]` 需要外部环境或业务决策

## 1. 完成定义

- [x] 原服务端目录已由 `Framework` 完整替代，运行代码和当前命令不存在失效引用。
- [x] `Proj/Example` 是独立 Go module，持有服务器入口、项目路由、配置、镜像和文档。
- [x] 根 `go.work` 同时纳管 Framework 和 Example。
- [x] Framework 提供可复用 HTTP 组装、项目路由扩展点和服务器生命周期。
- [x] 根 Node 脚本安全选择 `GO_PROJECT`，默认使用 Example。
- [x] Go test、coverage、race、vet、govulncheck、build 和 5 轮 benchmark 已执行。
- [x] MSFront unit test、Route Handler 授权契约、lint、typecheck、build 和 yarn audit 已执行。
- [x] CI、Dockerfile、README、环境示例和评估文档已适配新目录。

## 2. 优化清单

### V3-01 Framework 与多项目边界

- 优先级：P0
- 状态：`[x]`
- 已完成：
  - 服务端公共 module 改为 `github.com/zbxing/goexample/Framework`。
  - 原内部基础包提升为 `auth/config/health/httpapi/observability/server/validation` 公共包。
  - 创建 `Proj/Example` 独立 module，迁移入口、项目路由、Dockerfile、`.env.example` 和 README。
  - 创建根 `go.work`，纳管 `./Framework` 与 `./Proj/Example`。
- 验收：
  - `go list -m` 同时输出 Framework 和 Example。
  - 当前 README、脚本、CI、Framework、Proj 和 MSFront 中没有旧运行路径引用。
  - V1/V2 文档保留旧名称仅作为历史记录。

### V3-02 Framework 扩展点与生命周期

- 优先级：P0
- 状态：`[x]`
- 已完成：
  - 新增 `RouteRegistrar`、`Options.Endpoints`、`Options.RegisterRoutes`。
  - 新增 `DefaultEndpoints`、`RegisterDefaultRoutes`、`Success`、`Failure`。
  - 新增 `Framework/server`，集中 listener、draining、传播等待和 shutdown。
  - listener 改为同步绑定，只有绑定成功后才记录 `server_started`。
  - Example 入口只加载配置、组装依赖、注册项目路由并调用 Framework。
  - Example 新增 `GET /api/v1/project`，验证项目可以扩展默认 V1 API。
- 验收：
  - 自定义路由测试通过。
  - 真实 socket 启停、draining、取消与端口占用失败测试通过。

### V3-03 配置与 HTTP 语义加固

- 优先级：P1
- 状态：`[x]`
- 已完成：
  - `APP_ENV` 统一转小写并只允许 development、test、production。
  - 非法环境值在启动前失败，不能绕过 production 保护。
  - `/api/system/info` 添加 `Cache-Control: no-store`。
  - 保持统一错误 envelope、JSON media type、最小权限 CORS、探针和 metrics no-store。
- 验收：
  - 配置与 HTTP smoke 测试通过。
  - system info 缓存头测试通过。

### V3-04 多项目 Node 编排

- 优先级：P0
- 状态：`[x]`
- 已完成：
  - 根 Go 脚本统一为 `scripts/go-project.mjs`。
  - 默认项目为 Example，可通过 `GO_PROJECT` 选择 `Proj/*`。
  - 项目名使用字符白名单，并校验解析路径仍位于 `Proj` 下。
  - 目标目录必须包含 `go.mod`。
  - test、cover、race、vet 同时覆盖 Framework 和目标项目。
  - govulncheck 在 Framework 和目标项目各自 module 根目录顺序执行。
  - build/run 只面向目标项目。
- 验收：
  - 根 Yarn 命令全部可用。
  - `GO_PROJECT=../escape` 被快速拒绝，未执行 Go。

### V3-05 CI、镜像与供应链复现

- 优先级：P1
- 状态：`[x]`，镜像本机运行验证为 `[-]`
- 已完成：
  - Go CI 执行 test、race、vet、分 module govulncheck 和 build。
  - Go CI 通过 `go work edit -json` 自动发现全部 module 与 `Proj/*`，使用动态 matrix 验证。
  - 每个带 Dockerfile 的项目在 CI 构建镜像，并通过 `/livez` 与 `/readyz` smoke。
  - 新增 MSFront CI，执行 `yarn install --frozen-lockfile --non-interactive`、production audit、lint、typecheck 和 build。
  - 新增根 Node 工具 CI，执行编排脚本边界测试。
  - checkout、setup-go、setup-node 固定完整 commit SHA。
  - workspace 与两个 module 显式使用 `toolchain go1.25.13`，setup-go 与 Docker 使用同一补丁线。
  - Docker Go builder 与 distroless runtime 固定 OCI digest。
  - Docker 上下文改为仓库根，只复制 Framework 与 Example；镜像内使用 `GOWORK=off`，不受未来其他项目影响。
  - 新增根 `.dockerignore`，`go.work` 不再被忽略。
- 验收：
  - actionlint 校验三份 workflow 通过。
  - Dockerfile 和上下文已静态审计。
  - `[-]` 本机没有 Docker CLI，本轮不能实际 build/run；CI smoke 已配置，仍需远端 workflow 运行证据。

### V3-06 MSFront 生产配置与依赖

- 优先级：P1
- 状态：`[x]`
- 已完成：
  - production 签发/验证会话前拒绝缺失、默认或少于 32 字符的 `MSFRONT_JWT_SECRET`。
  - `jwtVerify` 固定允许 HS256，并验证 issuer/audience/expiration。
  - 登录接口记录内部错误，但客户端只收到通用 500 消息。
  - Cookie 保持 HttpOnly、production Secure、SameSite=Lax 和 Path=/。
  - `/api/management/*` 与 `/api/system/*` 统一执行 `requireApiAccess` 服务端角色授权。
  - TypeScript AST 契约逐个验证所有受保护的导出 HTTP handler 使用自身 Request 授权。
  - 更新 Next 16.3.1、React/ReactDOM 19.2.8、eslint-config-next 16.3.1。
- 验收：
  - lint、typecheck、production build 通过。
  - `yarn audit` 和 `yarn audit --groups dependencies` 均为 0 漏洞。

### V3-07 测试、并发与性能回归

- 优先级：P1
- 状态：`[x]`
- 已完成：
  - 全量 Go test、coverage、race、vet、build 通过。
  - MSFront 新增 Vitest Node 单元测试，覆盖 Cookie、session、401/403/allow 和关键筛选工具。
  - Go package 测试保持就近放置；MSFront 单测迁入 `MSFront/__tests__/unit`；根 `__test__` 按 Node、集成、契约、E2E、fixtures 和 reports 分类。
  - 根 Node 编排脚本新增 2 个边界测试，`yarn test` 统一执行 Node、Go 与 MSFront 测试。
  - Windows race 使用 `E:/DevTools/GCC/mingw64/bin` 的 WinLibs GCC。
  - 使用相同 Windows/amd64、50,000 次、5 轮中位数复测。
  - 未在缺少 Linux/TLS/代理/I/O 证据时开启 Prefork、Immutable、ReduceMemoryUsage、任意 concurrency 或 PGO。
- 覆盖率：

| 包 | Statement coverage |
| --- | ---: |
| Framework/auth | 82.1% |
| Framework/config | 91.1% |
| Framework/health | 94.1% |
| Framework/httpapi | 84.7% |
| Framework/observability | 83.7% |
| Framework/server | 75.6% |
| Framework/validation | 68.4% |
| Proj/Example/cmd/server | 65.0% |
| Proj/Example/internal/projectapi | 100.0% |
| **组合总计** | **85.6%** |

- 基准：

| Benchmark | V3 前 | V3 后 | 表面变化 | V3 后 B/op / allocs |
| --- | ---: | ---: | ---: | ---: |
| Hello 串行 | 42,654 ns/op | 44,049 ns/op | +3.3% | 16,497 / 110 |
| Hello 并行 | 13,650 ns/op | 14,552 ns/op | +6.6% | 16,883 / 111 |
| Metrics 并行 | 5,029 ns/op | 6,230 ns/op | +23.9% | 10,986 / 31 |
| Readiness | 185.3 ns/op | 191.4 ns/op | +3.3% | 336 / 2 |

- 解释：
  - metrics 五轮范围为 5,081-7,194 ns/op，波动较大，当前数据不足以判定真实回归。
  - V3 没有改变这些热路径的核心算法，内存与 allocation 仍处于原量级。
  - 后续必须在稳定 Linux runner 保存原始样本并用 benchstat 判定。

### V3-08 漏洞扫描的多 module 修复

- 优先级：P0
- 状态：`[x]`
- 问题：
  - 只有 `go.work` 的仓库根不能作为 govulncheck 扫描 module，原命令报 `no go.mod file`。
- 已完成：
  - Node 脚本依次在 `Framework` 与 `Proj/<GO_PROJECT>` 执行 govulncheck。
  - Go CI 同步改为两个 `go -C ...` 命令。
- 验收：
  - 两个 module 均报告 0 个可达漏洞。
  - module 图存在 1 个未被当前代码调用的漏洞，已保留风险说明。

### V3-09 文档、评分与迁移说明

- 优先级：P1
- 状态：`[x]`
- 已完成：
  - 覆盖重写 `docs/评估/项目架构与性能评估.md`。
  - 评估覆盖根 Node、Framework、Example、MSFront、CI、Docker 和 docs。
  - 使用 12 个维度、权重和证据评分；去掉分享评分。
  - 综合评分更新为 **8.9/10**，明确列出距 10 分差距。
  - 更新根、Framework、Example 和 MSFront README。
  - 本文回填实施、验证、基准和外部限制。

### V3-10 Yarn 环境、前端授权与多项目交付补强

- 优先级：P1
- 状态：`[x]`，需要目标环境的交付证明为 `[-]`
- 已完成：
  - 根命令统一为 `yarn xxx`，`yarn env` 安装根依赖、Go 工具链/module 与 MSFront 依赖。
  - 根与 MSFront 使用 Yarn v1 锁文件，CI 使用 frozen lockfile 与 Yarn cache。
  - management Route Handler 服务端授权统一，37 个受保护方法通过 AST 契约检查。
  - MSFront 新增 3 个 Vitest 文件、12 个用例，并接入根命令与 CI。
  - 根 Node 工具测试和独立 CI 已建立；跨项目集成、契约和 E2E 目录已预留。
  - Go CI 自动发现 `go.work` module/project；项目构建和 Docker health smoke 使用 matrix。
- 验收：
  - `yarn env`、`yarn test`、lint、typecheck、build 与 audit 通过。
  - actionlint v1.7.12 校验三份 workflow 通过。
  - `[-]` Docker、签名、provenance 与部署验证仍需要具备 Docker/发布权限的目标环境。

## 3. 验证记录

| 命令/检查 | 结果 |
| --- | --- |
| `yarn test:server` | 通过 |
| `yarn cover:server` | 通过，85.6% |
| `yarn race:server` | 通过，无数据竞争 |
| `yarn vet:server` | 通过 |
| `yarn vuln:server` | 通过，0 个可达漏洞 |
| `yarn build:server` | 通过 |
| 固定 50,000 次、5 轮 benchmark | 完成 |
| `yarn env` | 通过，根/Go modules/MSFront 环境就绪 |
| `yarn test` | 通过，根 Node、Framework、Example 与 MSFront 全部成功 |
| `yarn test:node` | 通过，2 个编排脚本边界测试 |
| `yarn test:front` | 通过，3 个文件、12 个单测、37 个授权契约 |
| `yarn lint:front` | 通过 |
| `yarn typecheck:front` | 通过 |
| `yarn build:front` | 通过 |
| `yarn audit` | 0 漏洞 |
| `yarn audit --groups dependencies` | 0 漏洞 |
| actionlint v1.7.12 | 通过，三份 workflow |
| 非法 GO_PROJECT | 正确拒绝 |
| Docker build/run | 未执行，本机无 Docker CLI |

## 4. 尚未完成：达到 10 分所需工作

这些内容需要业务模型、基础设施或目标环境，不能在模板中伪造完成：

- `[-]` 目标 Linux 容器、TLS、代理、真实数据库/Redis/消息系统的容量、故障和长稳压测。
- `[-]` limiter/idempotency 共享 Storage、分布式 Lock、租户/用户 key scope。
- `[-]` 正式 OIDC/OAuth2、RBAC/ABAC、token rotation/revocation、MFA 和安全审计。
- `[-]` OpenTelemetry Collector、采样、脱敏和 trace/metric/log 关联。
- `[-]` PostgreSQL schema/migration/事务、连接池容量与备份恢复演练。
- `[x]` MSFront management/system Route Handler 服务端授权统一，并由 AST 契约持续检查。
- `[~]` MSFront 单元测试已建立；真实 Route Handler 集成、Playwright E2E、可访问性和 Core Web Vitals 仍未完成。
- `[~]` 多项目 CI matrix 与 Docker smoke 已配置；SBOM、签名、provenance、镜像扫描、远端运行证据和部署演练仍未完成。
- `[-]` OpenAPI、稳定错误码、契约兼容和 API inventory 自动化。

## 5. V3 结论

V3 的仓库结构迁移、Framework 扩展、Example 工程、Yarn 环境、MSFront 授权与单测、动态 CI、配置、依赖安全和本机质量门禁均已完成。当前综合评分为 **8.9/10**；剩余 1.1 分主要对应真实生产系统、浏览器端质量和供应链交付证明，不属于可以用示例代码假装完成的工作。
