# 待优化 V6

评估日期：2026-08-17  
来源：V5 后全仓复扫、PostgreSQL repository 查询、Fiber 实际路由、OpenAPI/供应链官方资料及本地全量验证  
目标：优先闭环数据一致性、API 单一事实源和供应链 inventory；不以文档替代目标环境证据

## 1. 本轮已完成

### V6-01 PostgreSQL 初始 schema

- 优先级：P0
- 状态：`[x]`
- 修改：
  - 新增 `MSFront/database/migrations/0001_initial.sql`。
  - 覆盖 `managed_projects`、`framework_users`、`msfront_managed_roles`、`auth_sessions`、`framework_api_keys`、`audit_events`。
  - 增加 PK、FK、CHECK、时间、version、JSONB 类型、活动凭据和审计查询索引。
  - `managed_projects.code` 与活动用户名使用大小写不敏感唯一索引，消除应用层先查后写的竞态窗口。
- 验收：migration 文件命名、非空和 SHA-256 测试通过；真实 PostgreSQL DDL 尚未执行。

### V6-02 事务型 migration runner

- 优先级：P0
- 状态：`[x]`
- 修改：
  - 新增 `MSFront/scripts/migrate.mjs` 和无副作用 helper。
  - 使用 PostgreSQL session advisory lock 防止并发 runner。
  - 记录 migration version、SHA-256、执行耗时和应用时间。
  - 每个 migration 独立 `BEGIN/COMMIT`，失败执行 `ROLLBACK`。
  - 已应用文件 checksum 改变时拒绝继续执行。
  - 增加根命令 `yarn migrate:front`、`--status`、`--dry-run`。
- 验收：`yarn migrate:front --dry-run` 输出 `0001_initial` 与 64 位十六进制 checksum；helper 4 个单测通过。

### V6-03 PostgreSQL 连接池边界

- 优先级：P0
- 状态：`[x]`
- 修改：
  - `MSFRONT_DB_POOL_MAX`，默认 10，范围 1-100。
  - `MSFRONT_DB_CONNECT_TIMEOUT_MS`，默认 5000。
  - `MSFRONT_DB_IDLE_TIMEOUT_MS`，默认 30000。
  - `MSFRONT_DB_STATEMENT_TIMEOUT_MS`，默认 15000。
  - `MSFRONT_DB_QUERY_TIMEOUT_MS`，默认 20000。
  - 所有配置只接受有边界的安全整数；pool 监听 idle client error。
- 验收：默认值、覆盖值和非法值 4 组参数化测试通过；typecheck/lint 通过。

### V6-04 撤销与审计原子性

- 优先级：P0
- 状态：`[x]`
- 修改：
  - session/API Key 撤销改为显式 client transaction。
  - 撤销 UPDATE 与 audit INSERT 同事务提交。
  - 移除 `void recordAuditEvent(...)` 和空 `catch {}`，审计失败会回滚安全操作。
- 验收：typecheck、lint、生产构建通过；真实 PostgreSQL 故障注入仍待完成。

### V6-05 OpenAPI 3.1 单一事实源

- 优先级：P1
- 状态：`[x]`
- 修改：
  - 新增 `docs/openapi/openapi.json`，覆盖平台、健康、认证、示例和 Example 项目路由。
  - 文档包含 security schemes、request bodies、参数、通用 envelope 和错误 responses。
  - 删除重复的 `MSFront/data/api-inventory.json`。
  - MSFront inventory 直接解析标准 `paths`、operation security 和 components security schemes。
- 验收：17 paths、17 operations、17 个唯一 operationId；parser 3 个单测通过。

### V6-06 Fiber/OpenAPI 漂移契约

- 优先级：P1
- 状态：`[x]`
- 修改：
  - 新增 Go 契约测试，构建启用 demo auth 的完整 Fiber app。
  - 从 `app.GetRoutes(true)` 读取实际注册路由，不通过源码正则推测。
  - 精确比较 method/path 集合，检查 OpenAPI 3.1、operationId 唯一和 responses 非空。
  - OpenAPI 变更会同时触发 Go 和 MSFront workflow。
- 验收：`go test ./internal/projectapi` 通过，项目路由 package statement coverage 100%。

### V6-07 CycloneDX SBOM 与 Grype 门禁

- 优先级：P1
- 状态：`[x]`（实现完成，远端证据待补）
- 修改：
  - 新增独立 `supply-chain.yml`。
  - 使用 Anchore SBOM Action v0.24.0 生成 CycloneDX JSON artifact。
  - 使用 Anchore Scan Action v7.4.0 扫描 SBOM，High 及以上使构建失败。
  - checkout、SBOM 和 scan action 均固定完整 commit SHA；权限为 `contents: read`。
- 验收：官方 release/tag 和 SHA 已在线核对，actionlint 通过；本轮没有远端 Actions artifact，不能声称扫描已在 GitHub runner 成功。

### V6-08 断网漏洞扫描恢复

- 优先级：P1
- 状态：`[x]`
- 修改：
  - `yarn vuln:server` 优先使用 `GOVULNCHECK_BINARY` 或 `.temp/bin/govulncheck`。
  - 自动把当前 Go 工具链目录加入 scanner 子进程 PATH。
  - 本地 scanner 不存在时仍回退固定版本 `go run ...@v1.1.4`。
- 验收：在 `proxy.golang.org` 不可达时，Framework 和 Example 均扫描完成并报告 0 个可达漏洞；本地 DB 更新时间为 2026-08-14。

## 2. 本轮验证结果

| 检查 | 结果 |
| --- | --- |
| `yarn test` | 根 Node 4 用例、Go 全包、MSFront 11 文件/41 用例全部通过 |
| Route Handler contract | 37 个受保护方法及 JSON schema parsing 通过 |
| Fiber/OpenAPI contract | 17 个实际路由与 OpenAPI 精确一致 |
| `yarn cover:server` | 组合 statement coverage 85.6% |
| `yarn race:server` | Windows CGO + GCC 通过 |
| `yarn vet:server` | 通过 |
| `yarn vuln:server` | 两个 module 均为 0 个可达漏洞 |
| `yarn lint:front` | 通过，0 warning |
| `yarn typecheck:front` | 通过 |
| `yarn build:front` | Next 16.3.1 production build 通过 |
| production audit | 73 个生产依赖，0 漏洞 |
| migration dry-run | 1 个 migration、checksum 稳定 |
| actionlint | 4 个 workflow 通过 |

## 3. 仍需完成

### V6-09 真实 PostgreSQL integration 与恢复

- 优先级：P0
- 状态：`[-]`
- 要求：
  - 在 CI PostgreSQL service container 和 staging 执行空库、重复执行、checksum mismatch 和并发 runner 测试。
  - 覆盖项目 CRUD、用户/角色事务、session/API Key 撤销与 audit rollback 故障注入。
  - 执行连接耗尽、statement timeout、死锁/serialization failure 和进程中断测试。
  - 建立备份、PITR、恢复校验、RPO/RTO 和演练记录。

### V6-10 正式身份与分布式状态

- 优先级：P0
- 状态：`[-]`
- 要求：OIDC/OAuth2、MFA、短会话/刷新轮换、集中撤销、KMS 密钥轮换，以及 limiter/idempotency/lock 的 Redis 或等价共享 store。

### V6-11 OpenTelemetry 与 SLO

- 优先级：P1
- 状态：`[-]`
- 要求：OTLP exporter、Collector、trace propagation、日志/指标关联、敏感字段策略、SLI/SLO、dashboard 和告警演练。

### V6-12 浏览器与前端性能证据

- 优先级：P1
- 状态：`[-]`
- 要求：Playwright 关键权限流程、axe、错误恢复、视觉回归、bundle budget、Core Web Vitals 和客户端模块收敛。

### V6-13 容量、长稳与交付证明

- 优先级：P1
- 状态：`[-]`
- 要求：Linux/TLS/代理/真实数据库 I/O 压测、24h 长稳、故障注入、镜像 SBOM/扫描、cosign 签名、SLSA provenance、远端 workflow artifact 和部署/回滚/恢复记录。

## 4. 评分影响

- HTTP 与 API 契约：9.5 -> 9.8。
- 安全与身份：8.3 -> 8.5。
- 数据一致性与持久化：7.5 -> 8.5。
- MSFront 架构与性能：8.6 -> 8.8。
- 测试与静态质量：9.2 -> 9.4。
- CI 与供应链：8.8 -> 9.2。
- 扩展与部署能力：7.2 -> 7.4。
- 文档与可维护性：9.3 -> 9.6。
- 综合加权分：8.793 -> 8.993，按一位小数从 **8.8/10** 提升到 **9.0/10**。

## 5. 进入 V7 的准入条件

V7 不应继续用静态文件数量换分。至少应选择一个需要真实基础设施的闭环：

1. PostgreSQL service container + migration/repository integration；或
2. Playwright 权限与错误恢复主流程；或
3. OpenTelemetry Collector + SLO/告警；或
4. 目标 Linux 容量、长稳和故障注入。

在获得远端 Actions 与目标环境记录前，V6-09 至 V6-13 保持未完成状态。
