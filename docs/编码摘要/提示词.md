# GoExample 优化执行提示词

你正在维护 `GoExample`。目标是在不夸大能力、不伪造证据、不扩大既定范围的前提下，通过真实代码、测试、运行制品和目标环境验证提高项目综合评分。

## 一、不可变规则

1. 每轮只能执行下述“分支 A”或“分支 B”之一，二者互斥；不存在第三个“长期优化”分支。
2. 本轮优化范围明确排除 `MSFront/**`：不得修改其源码、配置、依赖、测试或文档，不得以其既有能力为本轮提分。根目录的共享改动也不得只为 `MSFront` 服务。
3. 评分提升必须来自可定位的实现差异及相称的验证证据。只改文档、增加声明、增加 workflow 或增加未成功运行的配置，不得提分。
4. 不得把本地单元测试、`httptest`、loopback、`miniredis`、本地容器或测试替身描述为目标环境、生产部署或远端运行证据。
5. 不得为追求 `10/10` 虚构完成状态、运行 URL、签名、审批、RPO/RTO、容量结论或兼容性结论。
6. 尊重现有未提交改动，不回退、不覆盖与当前任务无关的用户修改；避免无必要的大规模重构和文件膨胀。

## 二、执行前事实核验

每次开始都必须重新读取仓库事实，不能把本提示词中的“当前状态”当作永久真值。

1. 按版本号数值排序 `docs/待优化/待优化V*.md`，确定最新文档，读取其状态、范围、工作包、完成标准、停止规则、非目标和实施记录。
2. 读取 `docs/评估/项目架构与性能评估.md` 的当前评分、权重、证据边界和剩余短板。
3. 读取相关源码、测试、配置、`README.md`、`Framework/README.md`、`go.work` 和 `package.json`；文档与代码冲突时，以可运行代码和测试事实为准，并修正文档。
4. 遵守仓库 `AGENTS.md`：当 `graphify-out/graph.json` 存在时，先用 `graphify query` 查询本轮问题；需要关系或概念细节时使用 `graphify path` / `graphify explain`。修改后执行 `graphify update .`。
5. 如果进入全面重评，可查阅最新官方文档、标准、上游框架资料和社区实践；外部资料只能作为比较依据，不能替代仓库证据。

事实来源优先级：可运行代码与测试/制品 > 最新待优化文档的明确状态 > 当前评估文档 > README 与设计说明 > 外部资料 > 推测。

## 三、两个互斥执行分支

### 分支 A：最新待优化文档尚未完成

满足任一条件即进入分支 A：文档状态为“实施中”或等价状态；存在未达到 acceptance criteria 的工作包；严格 verifier 仍为 `not_recorded` / `failed`；完成条件要求的目标运行、签名、审批或原始制品尚未取得。

执行要求：

1. 只继续最新待优化文档已经冻结的范围，不创建下一版待优化文档，不覆盖重写项目架构与性能评估文档。
2. 按优先级、依赖顺序和剩余风险选择仍可在当前条件下真实推进的事项；优先修复能够关闭明确缺口并建立失败门禁的代码，而不是继续追加开放式愿望清单。
3. 完成代码、测试、验证和文档同步，将修改内容、命令结果、证据级别、限制及剩余项写回最新待优化文档。评估文档可同步事实和有依据的分数变化，但不得伪装成一次新的全面重评。
4. 只有全部完成标准都满足，或文档预先定义的“允许终止接受条件”已经由授权方批准并留下制品时，才可将文档标记为完成。
5. 真实目标环境、外部 IdP、签名基础设施、生产审批等不可控条件暂不可用时，不要求伪造结果，也不能把对应工作包改成 `recorded` 或“已完成”。应记录 blocker、已完成的仓库内风险降低、残余风险和取得外部条件后的明确命令；若范围内已无可诚实推进的事项，本轮停止并报告阻塞，不创建下一版本。

### 分支 B：最新待优化文档已经完成

只有最新文档的全部完成条件均已满足，或所有例外均符合该文档预先定义且已获批的终止接受条件时，才进入分支 B。分支 B 是一个完整动作：重新评估、覆盖评估文档、创建下一版待优化文档，然后立即按新文档开始优化；不能只生成文档后结束。

执行要求：

1. 对除 `MSFront` 外的代码、架构、性能、安全、可靠性、兼容性、供应链、部署与证据链做全面复核，列出已验证优点、缺点、限制和风险。
2. 覆盖写入 `docs/评估/项目架构与性能评估.md`，不是在旧文末尾无限追加。保留必要历史结论，但正文必须反映当前实现和当前证据。
3. 继续沿用可比较的评分维度、权重和精确加权方式；如需改变模型，必须解释原因并给出新旧口径映射。每个分项使用 `x.x/10`，最后同时显示一位展示值和至少三位精确综合值。
4. 根据评估中尚未闭合的短板创建下一版 `docs/待优化/待优化V{N+1}.md`。把相关问题合并成数量适中的工作包，避免一项缺陷创建一个版本，也避免把无关问题塞进同一工作包。
5. 新文档创建时必须包含：版本边界、优先级、依赖顺序、逐项完成标准、所需证据、停止规则、非目标、允许终止接受条件、状态枚举、基础复核命令和实施记录区。
6. 新文档范围冻结后，立即选择最高优先级且可执行的工作包进行代码优化、测试和证据生成，并同步实施记录与评分。不得退回分支 A 之外再创建另一份新文档。

## 四、项目真实上下文

### 项目定位

`GoExample` 是一个以 Go 服务端工程能力为核心的多项目工作区，目标是提供可复用的生产级 Framework、可独立部署的业务/通用服务、版本化 OpenAPI 与生成式 Go SDK，以及可审计的测试、发布、部署和恢复证据链。不得用其它仓库的业务领域或技术栈替换这一定位。

### 工作区与模块边界

- 根目录：Node.js/Yarn 命令编排、契约检查、代码生成、证据生成与验证；不是业务运行时。
- `Framework/`：可复用 Go Framework，提供 HTTP、认证授权、共享状态、数据库、消息、可观测性和生命周期能力。
- `Solutions/Example/`：默认示例业务解决方案，持有 application use case、composition root、服务契约和标准 HTTP 入口。
- `Services/Billing/`：可被多个方案复用的独立 Billing 服务，不是 `Example` 内部包。
- `SDK/GoExample/`、`SDK/Billing/`：从各自 OpenAPI 契约生成并校验漂移的 Go SDK。
- `support/consumer/HealthProbe/`：独立 SDK 消费者与健康端点迁移演练模块，不能替代真实外部消费者。
- `support/api-contracts/`、`contracts/`：外部 API 子模块和契约引用/版本治理。
- `support/deploy/`、`scripts/`、`__test__/`：部署基线、任务编排、跨模块契约及证据门禁。
- `MSFront/`：独立 Next.js 管理端，本提示词的优化范围明确排除。

`go.work` 当前使用 Go `1.25.0`、工具链 `go1.25.13`，包含六个模块：`Framework`、`Solutions/Example`、`Services/Billing`、`SDK/GoExample`、`SDK/Billing` 和 `support/consumer/HealthProbe`。

### 当前技术栈

- Go 1.25；Framework 内部使用 Fiber v3，并通过适配器暴露标准 `net/http.Handler`。
- 默认服务入口使用标准 `net/http` server/lifecycle；生产 application/composition 层不得直接依赖 Fiber。
- OpenAPI 契约、兼容性比较器和生成式 Go SDK。
- Redis/Redis Sentinel、PostgreSQL、NATS JetStream 的适配、契约测试与恢复证据工具。
- OpenTelemetry、Prometheus、结构化日志、健康探针和有界指标标签。
- Nginx、Kubernetes、CI、SBOM、release provenance、签名/部署证据与严格 manifest verifier。
- 根任务运行环境为 Node.js `>=20.19.0` 和 Yarn `1.22.22`。

### 已有能力基线

评估和优化不得忽略下列已有能力，也不得把它们夸大为生产闭环：

- Framework 与业务服务边界已建立；`Example` 和 `Billing` 是独立服务，生产组合根通过架构测试隔离 Fiber。
- 标准 HTTP 入口具备有界连接、请求、读写、停机、TLS/HTTP2、流式响应与 SSE 生命周期契约；正 SSE stream timeout 会通过标准 `ResponseController` 贯通到仍有界的传输写期限，零值保留服务器原预算；Fiber 兼容入口仍保留。
- typed query/command、统一 envelope、幂等、认证、角色及 tenant/resource 策略、OIDC/PKCE 和会话撤销等仓库内契约已建立。
- Redis 共享状态、Sentinel 客户端，`database/sql` 边界，broker-neutral queue 与 NATS JetStream adapter 已建立，并有本地/容器故障契约。
- 官方 OpenTelemetry、Prometheus 指标/规则、健康探针、审计 sink 边界及低敏日志/trace 契约已建立；server `traceparent` 使用定长规范编码，入站 W3C parent 由严格零分配帮助函数直接构造 remote SpanContext 而不建立 `propagation.MapCarrier`，合法 tracestate、flags 与现有拒绝策略保持不变；有效 parent 继续由单个 context wrapper 保留并在公开 `FromContext` 首读时缓存字符串结构，无 parent 时直接复用 OpenTelemetry 已返回的 span context，Fiber 响应头路径不生成中间字符串；NATS JetStream adapter 发布和 DLQ header 复制直接复用 `nats.NewMsg` 已分配的 map，避免二次 map 分配并保持控制 header 过滤；九种标准 HTTP 方法的 server span 启动配置在 middleware 构造阶段预计算，有限路由模板的最终名称/route 属性与 100–599 状态属性也在 middleware 实例中复用，自定义方法和越界状态保持动态回退；weak ETag 由等价 Q-polynomial CRC32 在固定栈缓冲中生成并由零分配/逐字节对照门禁保护；请求 deadline 通过 Go context 链携带 SSE 生命周期与标准写期限能力，避免普通请求使用 Fiber Locals 往返；示例 hello、delay 与 private 的固定 JSON 数据使用包内 typed response；hello 查询直接复用 Fiber 已解码字节，默认消息使用常量，自定义消息以预扩容 builder 构造；默认、空白和显式 `Fiber` 的完整成功 envelope 复用预编码 wire body，自定义名称继续走标准 JSON escaping，并由完整 body、Content-Type、ETag、解码、Unicode 空白及重复参数语义测试保护。
- OpenAPI 兼容门禁、双 SDK 生成/校验、消费者矩阵、Kubernetes/Nginx 渲染与校验、可复现 server release 和 evidence manifest/verifier 已建立。
- Fiber、原生 `net/http` 与 Framework 标准适配器已有固定本地 benchmark/capacity/soak 契约；这些结果不等于目标环境容量结论。

## 五、当前优化基线

截至本提示词最近同步时，最新文档是 `docs/待优化/待优化V14.md`，状态为“实施中”；V13 已于 2026-09-01 完结，但 V13-01 至 V13-09 未取得满足严格 verifier 的完整目标证据，已以 `not_recorded` 原状态迁移为 V14-07 至 V14-15。V14-01/V14-02 已完成标准 HTTP→Fiber 适配器的首轮仓库内优化与永久基准；V14-04 的仓库内部分已完成，直接 Fiber handler 覆盖 filtered/emitted request log、trace、metrics、激活 CORS/compression、ETag、有效 Bearer、idempotency 无 key/带 key 回放和 atomic 限流；parentless trace context 复用已有 span context，fingerprint 复用长度缓冲和锁 namespace key；queueclient 普通消息无 W3C trace header 时的 carrier 现为 `0 B/op、0 allocs/op`，带 trace header 仍为 `336 B/op、2 allocs/op`；queueclient 单 header 校验从旧实现 `8 B/op、1 alloc/op` 降为 `0 B/op、0 allocs/op`，多 header 重复检测保持原有安全边界；NATS JetStream 发布与 DLQ 复用 `nats.NewMsg` 自带 Header map，空/单/双业务 header 对照由 `48/416/432 B/op、1/3/4 allocs/op` 降至 `0/16/32 B/op、0/1/2 allocs/op`；NATS JetStream 接收转换对超过 8 个 header 提供容量提示，16/64 header 从旧实现 `8/12 allocs/op、2,144/9,376 B/op` 降至 `5 allocs/op、1,248/4,960 B/op`，空/单/双 header 分配保持不变。真实 Redis/IdP、固定 Linux 与目标负载仍为 `not_recorded`，尚不能声明达到 A级。当前评估展示综合评分为 `9.8/10`，精确加权值为 `9.827/10`，未舍入加权值为 `9.8265/10`；通用 evidence 输入合同为 250 项，其中 249 项必需；执行时必须以文件中的最新数值为准。

V13 的冻结工作包为：

1. V13-01：目标 edge、真实 DNS/证书及 HTTP/2/3、流式、断连、drain/rollback 实证。
2. V13-02：目标 Redis HA、TLS/ACL、故障切换、容量、备份恢复及获批 RPO/RTO。
3. V13-03：目标 IdP、MFA、claim/key/revoke/device 治理及生产策略引擎。
4. V13-04：生产审计持久化、完整性、KMS/Vault、留存、SIEM、paging 与责任闭环。
5. V13-05：目标 PostgreSQL/消息系统、真实业务资源、恢复、分区、DLQ/顺序/exactly-once 边界。
6. V13-06：固定远端 Linux benchmark/soak、可比 baseline、目标 payload/身份/依赖/TLS edge 与选型决策。
7. V13-07：远端 attestation、镜像签名、部署验签、目标集群 rollout/HPA/驱逐/NetworkPolicy/rollback。
8. V13-08：正式 SDK tag、外部消费者跨版本迁移和目标部署；第二个独立服务 `Billing` 仅完成了仓库内子项。
9. V13-09：目标长稳、资源耗尽、证书轮换、跨主机/跨区故障、告警响应及获批 RPO/RTO。

## 六、证据与评分纪律

所有结论必须标明证据层级：

| 层级 | 可证明内容 | 不可冒充内容 |
| --- | --- | --- |
| 静态/契约 | 结构、API、配置和失败门禁 | 运行性能、生产可用性 |
| 仓库内行为 | 单元、集成、真实 TCP、loopback 或本地容器行为 | 远端平台、生产拓扑和目标容量 |
| 远端 CI | 固定 runner 上的可追溯运行与制品 | 目标生产环境和业务审批 |
| 目标环境 | 指定版本、拓扑、身份和负载下的运行结果 | 未覆盖场景的普遍保证 |
| 审批闭环 | 具名角色批准的 RPO/RTO、例外和残余风险 | 技术实现本身 |

评分要求：

- 复用评估文档现有分项与权重，计算未舍入加权和，再生成展示值；禁止先决定目标分数再倒推分项。
- 每次加分必须指出“哪项缺口被关闭、对应代码/测试/制品是什么、为什么只增加该幅度、还剩什么限制”。
- 已为 `10.0` 的维度不得因同类增量重复加分；未取得目标证据的维度必须保守扣分。
- 性能结论必须标明“代码推断”“本地实测”“远端 CI 实测”或“目标环境实测”，并记录版本、机器/runner 指纹、参数、样本、误差/波动、原始输出和可比 baseline。
- `not_recorded` 只能表示未取得证据，不能夹带伪造的目标运行字段；`recorded` 必须通过对应严格 verifier。

## 七、实现与验证要求

1. 先复现缺口或建立失败测试，再做最小且结构清晰的实现；优先复用现有 package、脚本、schema、verifier 和测试模式。
2. 新增抽象必须解决真实重复或边界问题；避免为了提分进行表层重命名、无行为变化的拆分或大量新增文件。
3. 任何安全、并发、恢复、兼容或性能修改都必须包含负向/篡改测试和清晰的 timeout、容量或输入上限。
4. 只运行与改动风险相称的命令，但不得跳过直接受影响的门禁。服务器范围常用复核命令：

```powershell
yarn test:node
yarn test:server
yarn vet:server
yarn build:server
yarn api:compat
yarn contracts:check
yarn sdk:check --project Example
yarn sdk:check --project Billing
yarn sdk:matrix:check
$env:GO_PROJECT='Billing'; yarn build:server; Remove-Item Env:GO_PROJECT
```

5. 涉及对应证据链时，运行其生成器和严格 verifier，例如：

```powershell
yarn evidence:v13
yarn evidence:v13:verify
yarn evidence:manifest
yarn evidence:verify
```

还应按改动范围选择 `edge:*`、`redis:sentinel:*`、`postgres:recovery:*`、`nats:*`、`kubernetes:*`、`prometheus:*`、`release:server:*`、`sdk:*:evidence*` 或 `drill:server:*` 命令。未具备外部依赖时明确报告未运行原因，不能记为通过。

6. 完成修改后执行 `graphify update .`，复查 `git diff` 和 `git status`，确认没有触碰 `MSFront`、没有覆盖无关改动、没有把临时制品误纳入源码。
7. 最终报告必须说明：选择了哪个互斥分支、修改了什么、验证了什么、哪些命令未运行、评分是否及为何变化、剩余工作和外部 blocker。不要只报告“已优化”。

## 八、执行指令

最新的待优化文档全部完成了吗(不可抗因素除外，例如真实硬件或设备等等，可诚实推进的优化)，没有请继续；否则请重新详细全面评估覆盖写入项目架构与性能评估文档并生成新的待优化文档(MSFront除外，评分维度全面)，然后按照该文档对项目进行优化修改；两个步骤是互斥的，目标是提高综合评分，每次优化完更新分数

请给出本项目和主流服务器框架(包括Node，java等等)的详细性能比较，并且本项目综合性能在这些主流服务框架中处理什么水平
